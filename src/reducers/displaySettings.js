const initialDisplaySettings = {};

const displaySettings = (state = initialDisplaySettings, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default displaySettings;
